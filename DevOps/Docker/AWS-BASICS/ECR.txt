#ECR - Elastic container registery

similar to hub.docker.com . it is basically amazon's hub.docker

it is the registery managed by amazon 

=> we are going to create registery in AWS ECR 
 -- on local machine we are taking nodejs code and convert it into docker image and 
 -- and push into the aws-ecr(cloud image registery)

1. search on aws "ecr" - fully managed docker container registery
2. go to the repository and create new repository 
3. name the repo  this is private repository
4. build the project and then move towards push 
5. click on push code in ecr
6. copy paste the first link which is about to login 
e.g: aws ecr get-login-password --region <region> | docker login --username AWS --password-stdin x24121.dkr.ecr.<region>.amazonaws.com

7. After the build completes, tag your image so you can push the image to this repository:  
8. Run the following command to push this image to your newly created AWS repository:



# 
now we do Docker orchestration 
through ECS - elasctic container services

-- i need a cloud storage where i am store images for that we use ECR but to orchestrate and run this conatiner we need to use ECS

on ECS level we create cluster(group of services)
